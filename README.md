# Beer

Подумать, какой из двух интерфейсов нужно реализовать в классе BeerLover.
Добавить к классу BeerLover этот интерфейс и реализовать все его методы.
Подумать, как связаны переменная READY_TO_GO_HOME и метод isReadyToGoHome.
Вернуть значение переменной READY_TO_GO_HOME в методе isReadyToGoHome.

АЛГОРИТМ Пиво

    ГЛАВНЫЙ МЕТОД
        НИЧЕГО НЕ ДЕЛАТЬ

    ИНТЕРФЕЙС Пьющий
        МЕТОД попроситьЕще(сообщение: ТЕКСТ)
        МЕТОД сказатьСпасибо()
        МЕТОД готовИдтиДомой() -> ЛОГИЧЕСКОЕ

    ИНТЕРФЕЙС Алкоголик РАСШИРЯЕТ Пьющий
        КОНСТАНТА: ГОТОВ_ИДТИ_ДОМОЙ = ЛОЖЬ
        
        МЕТОД спатьНаПолу()

    КЛАСС ЛюбительПива РЕАЛИЗУЕТ Алкоголик
        МЕТОД попроситьЕще(сообщение: ТЕКСТ)
            НИЧЕГО НЕ ДЕЛАТЬ
        
        МЕТОД сказатьСпасибо()
            НИЧЕГО НЕ ДЕЛАТЬ
        
        МЕТОД готовИдтиДомой() -> ЛОГИЧЕСКОЕ
            ВОЗВРАТ Алкоголик.ГОТОВ_ИДТИ_ДОМОЙ  // Всегда возвращает ЛОЖЬ
        
        МЕТОД спатьНаПолу()
            НИЧЕГО НЕ ДЕЛАТЬ
